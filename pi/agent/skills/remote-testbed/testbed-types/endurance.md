# Endurance access

Use the endpoint and any jump/SSH settings returned by RAS. Confirm the host with
a shallow read-only probe before mutation. Firmware logs normally live under
`/var/log/pure`; determine exact filenames before searching or transferring files.
