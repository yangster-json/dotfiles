# subspec: <task-id> — <title>

<!-- generated by sdd-subspec-writer from spec.md + tasks.md + amendments at
     implement setup. this is the ONLY spec document the implementer and
     verifier for this task read — it must stand alone. -->

feature: <feature-slug>
task: <task-id>
complexity: standard <!-- simple | standard -->
verify: `<shell command that objectively passes or fails>`

## feature context
<!-- copied once from spec.md — Summary, Non-goals, Constraints. gives the
     implementer the feature's intent for micro-decisions without exposing
     other tasks' requirements. amendments rarely touch these; when one
     does, the subspec is regenerated. -->
<the spec's Summary paragraph>

non-goals:
- <excluded behavior>

constraints:
- <constraint the implementation must honor>

## requirements (full text)
<!-- copied verbatim from spec.md — every requirement this task implements -->
- **R<n>**: WHEN <trigger>, the system SHALL <observable behavior>.

## amendments touching this task
<!-- copied verbatim from state.md; amendments OVERRIDE requirements on
     conflict. omit the section when none apply. -->
- **A<n>** (<date>): <decision>

## files (the complete allowed set)
<!-- exact paths, tests included — touching anything else is a scope
     violation -->
- <path>

## interfaces
<!-- grepped verbatim from the source at subspec time: the exact
     signatures, types, and registration macros this task calls or
     implements (from its file list and its dependencies' outputs).
     contracts up front so a fresh-context implementer doesn't burn tokens
     re-deriving them; the verifier's wired check greps callers of these.
     omit the section when nothing applies. -->
- `<return_type function_name(args)>` — <path:line>

## notes
<what to build, precisely enough that a fresh-context implementer needs no
follow-up questions: behavior, integration points, test expectations —
from the task's body in tasks.md, plus any applicable learnings>
