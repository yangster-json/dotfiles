- [research-workflow-args-stringified](research-workflow-args-stringified.md) — stage:research tags:workflow,args,fallback — Workflow `args` can arrive as a JSON string, so a bare destructure throws at 0 ms with 0 agents. Fixed in research.js/review.js 2026-07-31; new workflow scripts need the same guard.
- [plan-gate-hook-flags-angle-brackets](plan-gate-hook-flags-angle-brackets.md) — stage:research tags:pipeline,hooks,gate1 — pre-gate-1 guard rejects sdd-state log messages containing ASCII arrows, and any redirect whose target is a shell variable. Avoid angle brackets in quoted prose; use Read/Write tools for appends outside the tree.
- [sdd-state-set-clobbers-empty-valued-key](sdd-state-set-clobbers-empty-valued-key.md) — stage:implement tags:pipeline,sdd-state,data-loss — `sdd-state set` on a field with an EMPTY value (e.g. base_ref) writes onto the next line and destroys the following key, while still exiting 0. Use Edit on state.md for blank fields.
- [per-task-verification-cannot-catch-a-wrong-requirement](per-task-verification-cannot-catch-a-wrong-requirement.md) — stage:review tags:verification,review,test-design,spec-quality — 9 verifier passes and 30+ mutation tests all green on a regression, because the requirement mandated it and the subspec's test corrupted all replicas so precedence was unobservable. Run the adversarial review hardest when the implement stage is clean; use asymmetric fixtures for any "prefer A over B" rule; grep for computed-but-never-read fields; re-review fix rounds.
- [split-predicate-and-loop-hides-off-by-one](split-predicate-and-loop-hides-off-by-one.md) — stage:implement tags:subspec-authoring,off-by-one,per-task-verification — a depth guard and the loop calling it were split across two subspecs with off-by-one conventions; both halves verified clean, the test froze the bug, and the walk fetched nothing. State the boundary variable's semantics in BOTH subspecs.
