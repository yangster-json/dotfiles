# base spec: <area>

<!-- living spec — the as-built behavior contracts of this area, merged
     from feature specs by /sdd:cleanup after each PR merges. one
     requirement per bullet, EARS shape, unique <AREA>-R<n> ids that are
     never reused; the provenance comment names the feature that
     established the line. the spec-writer and the consistency critic
     read this: a new feature that changes a contracted behavior must
     name the <AREA>-R<n> it supersedes — /sdd:cleanup then replaces the
     line and notes the supersession in its provenance. -->

- **<AREA>-R1**: WHEN <trigger>, the system SHALL <observable behavior>.
  <!-- from <feature-slug> R<n>, <YYYY-MM-DD>, <FW-XXXXX|no-jira> -->
